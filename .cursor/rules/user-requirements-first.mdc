---
description: 实质性工作前先读取仓库根目录 USER_REQUIREMENTS.md，并以其中需求为项目内最高优先
alwaysApply: true
---

# 用户书面需求优先

在进行**规划**、**修改代码**、**运行命令**，或回答与本仓库**实现/行为/架构**相关的实质性问题之前：

1. **必须先**用 Read 工具读取仓库根目录的 `USER_REQUIREMENTS.md`（与 `.cursor/rules/` 同级路径下的该文件名）。
2. **对齐优先级**：该文件中的条目优先于助手对项目的默认假设与惯例；若与系统安全、法律或工具强制约束冲突，以更高层约束为准。
3. **与全局用户规则**：若 `USER_REQUIREMENTS.md` 中声明了与 Cursor 全局「用户规则」的冲突裁决方式，从其声明；否则以 `USER_REQUIREMENTS.md` 为准。
4. **与其它 alwaysApply 项目规则**：若需求文件与某条项目规则（如弹窗提问、禁止本机构建等）冲突，以**更具体且仍生效的项目规则**为准；若你希望需求文件覆盖它们，请在 `USER_REQUIREMENTS.md` 中明确写出并相应调整或移除冲突的规则文件。
5. **文件不存在**：先在回复中说明该文件缺失，请用户创建或恢复；在未读取到内容前，不要编造该文件中的要求。

纯闲聊、与仓库无关的通用问题可不必读取该文件。
